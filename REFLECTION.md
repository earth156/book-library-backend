## ส่วนที่ท้าทายที่สุดสำหรับผมในโปรเจกต์นี้คือ การจัดการ 401 Interceptor Redirect Loop
- ตอนแรกผมตั้งค่า Axios Interceptor ให้ดักจับ Response 401 เพื่อ redirect ผู้ใช้ที่ Token หมดอายุ กลับไปหน้า Login
- แต่กลายเป็นว่าเมื่อผู้ใช้กรอกรหัสผิด API ก็ส่ง 401 กลับมาเช่นกัน ทำให้เกิด Infinite Loop รีไดเรกต์ซ้ำๆ โดยไม่แสดง Error Message
- ความยากคือมันเป็น Silent bug ที่ต้องทำความเข้าใจทั้ง Lifecycle ของ Axios และ React Router ไปพร้อมกัน
- ผมแก้ปัญหานี้ด้วยการเพิ่มเงื่อนไขตรวจสอบ ว่าถ้าเป็นการยิง Request ไปที่ /api/login ไหม ถ้าใช่ จะข้ามการทำงานส่วนนี้ไป
- จุดนี้ทำให้ผมได้เรียนรู้ว่าการจัดการ HTTP Status ต้องคำนึงถึงบริบท ของแต่ละ Endpoint อย่างรอบคอบครับ